#include <assert.h>
#include <stdio.h>
#include <stdlib.h>

#include "List.h"

int sumWhile(List l);
int sumFor(List l);

int main(int argc, char **argv) {
    List l = createList(argc, argv);
    freeList(l);
    return 0;
}

int sumWhile(List l) {
    // TODO
    return -1;
}

int sumFor(List l) {
    // TODO
    return -1;
}